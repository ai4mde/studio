from django.contrib import admin
from {{app_name}}.models import{% for model in models %} {{model.name}}{% if not loop.last %},{% endif %}{% endfor %}
{% for model in models %}
admin.site.register({{model.name}}){% endfor %}

